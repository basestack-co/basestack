import { colors } from "../index";

const navigation = {
  backgroundColor: colors.gray800,
  divider: {
    backgroundColor: colors.gray600,
  },
  button: {
    underline: colors.blue400,
  },
};

const settingsCard = {
  danger: {
    border: colors.red500,
  },
};

export { navigation, settingsCard };
