{
  "name": "@basestack/emails",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "start": "email dev --port 3333 --dir ./src/templates",
    "test": "jest",
    "test:watch": "jest --watchAll",
    "test:coverage": "jest --coverage",
    "test:log": "jest --no-coverage >& ./logs/jest.log",
    "lint": "eslint ./src",
    "lint:tsc": "tsc --noEmit",
    "clean": "rm -rf .turbo && rm -rf node_modules && rm -rf dist",
    "build": "email export"
  },
  "devDependencies": {
    "@basestack/config": "*",
    "@basestack/tsconfig": "*",
    "@react-email/components": "0.0.7",
    "react-email": "1.9.4",
    "ts-jest": "^29.1.0",
    "typescript": "^5.1.3"
  }
}
