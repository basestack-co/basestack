import styled from "styled-components";

export const Stars = styled.div`
  position: absolute;
  inset: 0;
  z-index: 1;
  overflow: hidden;
  width: 100%;
  pointer-events: none;
`;

export const Star = styled.span`
  position: absolute;
`;
