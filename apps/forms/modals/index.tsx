import React, { Fragment } from "react";
import CreateFormModal from "./Form/Create";

const Modals = () => {
  return (
    <Fragment>
      <CreateFormModal />
    </Fragment>
  );
};

export default Modals;
